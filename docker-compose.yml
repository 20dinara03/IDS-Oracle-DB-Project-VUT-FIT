version: '3.7'

volumes:
   pg-data:


services:
  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: xids00
      POSTGRES_PASSWORD: gdebody
      PGDATA: /pgdata
    ports:
      - "5432:5432"
    volumes:
        - pg-data:/pgdata

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
